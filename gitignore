# Создано https://www.gitignore.io/api/xcode,macos,swift
# Изменить на https://www.gitignore.io/?templates=xcode,macos,swift

### macOS ###
# Общая
.DS_Store
.AppleDouble
.LSOverride

# Значок должен заканчиваться двумя \ r
Икона

# Миниатюры
._ *

# Файлы, которые могут появиться в корне тома
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# Каталоги, потенциально созданные на удаленном ресурсе AFP
.AppleDB
.AppleDesktop
Сетевая папка для мусора
Временные предметы
.apdisk

### Swift ###
# Xcode
#
# Участники gitignore: не забудьте обновить Global / Xcode.gitignore, Objective-C.gitignore & Swift.gitignore

## Построение сгенерировано
сборки /
DerivedData /

## Различные настройки
* .pbxuser
! default.pbxuser
* .mode1v3
! default.mode1v3
* .mode2v3
! default.mode2v3
* .perspectivev3
! default.perspectivev3
xcuserdata /

## Другой
* .Moved зарезервированных
* .xccheckout
* .xcscmblueprint

## Obj-C / Swift специфичный
* .hmap
* .ipa
* .dSYM.zip
* .dSYM

## Детские площадки
timeline.xctimeline
playground.xcworkspace

# Swift Package Manager
# Добавьте эту строку, если вы хотите избежать проверки исходного кода из зависимостей Swift Package Manager.
# Пакеты /
# Package.pins
# Package.resolved
.build /
# Добавьте эту строку, если вы хотите избежать проверки интеграции Xcode SPM.
# .swiftpm / xcode

# CocoaPods
# Мы не рекомендуем добавлять каталог Pods в ваш .gitignore. тем не мение
# вы должны судить сами, плюсы и минусы упоминаются по адресу:
# https://guides.cocoapods.org/using/using-cocoapods.html#should-i-check-the-pods-directory-into-source-control
# Стручки /
# Добавьте эту строку, если вы хотите избежать проверки исходного кода из рабочего пространства Xcode
# * .xcworkspace

# Карфаген
# Добавьте эту строку, если вы хотите избежать проверки исходного кода из зависимостей Карфагена.
# Карфаген / Оформить заказ

Карфаген / сборки

# Accio управление зависимостями
Зависимость /
.accio /

# fastlane
# Рекомендуется не хранить скриншоты в git-репо. Вместо этого используйте fastlane, чтобы заново сгенерировать
# скриншоты, когда они нужны.
# Для получения дополнительной информации о рекомендуемом посещении установки:
# https://docs.fastlane.tools/best-practices/source-control/#source-control

Fastlane / report.xml
Fastlane / Preview.html
Fastlane / скриншоты / ** / *. PNG
Fastlane / test_output

# Внедрение кода
# После новых инструментов внедрения кода появляется сгенерированная папка / iOSInjectionProject
# https://github.com/johnno1962/injectionforxcode

iOSInjectionProject /

### Xcode ###
# Xcode
# Участники gitignore: не забудьте обновить Global / Xcode.gitignore, Objective-C.gitignore & Swift.gitignore

## Пользовательские настройки

## совместимость с Xcode 8 и более ранними версиями (игнорирование не требуется, начиная с Xcode 9)

## совместимость с Xcode 3 и более ранними версиями (игнорирование не требуется, начиная с Xcode 4)

## Xcode Patch
* .Xcodeproj / *
! *. Xcodeproj / project.pbxproj
! *. Xcodeproj / xcshareddata /
! *. Xcworkspace / contents.xcworkspacedata
/*.gcno

### Xcode Patch ###
** / xcshareddata / WorkspaceSettings.xcsettings

# Конец https://www.gitignore.io/api/xcode,macos,swift
